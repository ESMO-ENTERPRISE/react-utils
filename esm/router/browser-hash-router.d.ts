import { type ReactNode } from 'react';
type BrowserHashRouterProps = {
    children?: ReactNode;
};
declare const BrowserHashRouter: ({ children }: BrowserHashRouterProps) => JSX.Element;
export { type BrowserHashRouterProps, BrowserHashRouter };
